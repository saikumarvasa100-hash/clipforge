# =============================================================
# ClipForge -- Requirements (100% FREE, zero paid APIs)
# =============================================================

# Core framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# Task queue
celery[redis]>=5.3.0
redis>=5.0.0

# Database (local SQLite -- no cloud dependency)
sqlalchemy>=2.0.0
aiosqlite>=0.19.0
alembic>=1.13.0
greenlet>=3.0.0

# Local Auth (replaces Supabase)
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0
python-multipart>=0.0.6

# Environment
python-dotenv>=1.0.0
pydantic-settings>=2.0.0

# AI (local Whisper + HERMES bridge to free OpenRouter models)
faster-whisper>=1.0.0
# OpenRouter free models via HERMES agent (already installed at :7890)

# Audio / Video
ffmpeg-python>=0.2.0
librosa>=0.10.0
scipy>=1.12.0
numpy>=1.26.0
opencv-python-headless>=4.9.0

# YouTube / Video download
yt-dlp>=2024.1.0

# Browser automation for publishing (replaces TikTok/FB/LinkedIn APIs)
playwright>=1.40.0
# NOTE: Run 'playwright install chromium' after pip install

# Storage (local filesystem, no boto3/R2 needed)
# boto3 removed -- using local_storage.py

# HTTP
httpx>=0.27.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.12.0
